name: produk_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0  # Tambahkan ini
  intl: ^0.19.0  # Tambahkan ini untuk format Rupiah
  mobile_scanner: ^5.1.0  # Tambahkan ini untuk fitur scan barcode
  
  # TAMBAHAN BARU:
  fl_chart: ^0.66.0   # Untuk Grafik Canggih
  pdf: ^3.10.0        # Untuk Membuat Dokumen PDF
  printing: ^5.11.0   # Untuk Print/Share PDF

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true